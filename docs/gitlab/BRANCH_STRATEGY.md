# Branch strategy
`main` is releasable, `develop` integrates, and `feature/<area>` branches isolate work.
Hessam owns backend/DevOps branches; Pourya owns frontend/docs branches; UML and final polish
are shared. Branches remain after merge as academic history.
