# Push commands
```bash
git push -u origin main
git push -u github main
git push -u origin develop
git push -u github develop
git push origin --all && git push github --all
git push origin --tags && git push github --tags
```
