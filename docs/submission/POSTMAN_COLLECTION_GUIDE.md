# Postman guide
Create environment variables `baseUrl`, `token`, `eventId`, `seatId`, `reservationId`,
`paymentId`, and `ticketToken`. Import curl examples from API_DEMO_GUIDE, then add a login
test that saves `pm.response.json().accessToken` to `token`. Run in documented order and keep
failure/timeout requests as separate examples.
