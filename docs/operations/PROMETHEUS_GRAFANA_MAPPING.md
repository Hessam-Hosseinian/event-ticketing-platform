# Prometheus/Grafana mapping
`http_requests_total` and duration histograms feed API RED panels; `seat_lock_attempts_total`
shows conflict/error ratios; reservation/payment counters form a checkout funnel;
`waiting_room_depth`, RabbitMQ queue gauges, PostgreSQL latency, pod CPU/memory and restarts
form capacity panels. Dashboard links accompany alerts.
