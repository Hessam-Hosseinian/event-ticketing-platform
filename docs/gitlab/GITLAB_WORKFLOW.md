# GitLab workflow
Issues carry acceptance criteria and link to a feature branch. Feature branches target
`develop`; CI and one peer review are required. The stable `main` receives only the final
reviewed integration merge and release tag. Never force-push protected branches.
